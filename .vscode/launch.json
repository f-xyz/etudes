{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Etudes",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/etudes",
      "args": [],
      "cwd": "${workspaceFolder}",
      "targetArchitecture": "x64",
      "stopAtEntry": false,
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "preLaunchTask": "CMake: build",
      "setupCommands": [
        {
          "text": "-enable-pretty-printing",
          "description": "Enable pretty-printing for gdb",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
